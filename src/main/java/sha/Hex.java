package sha;

public class Hex {
  private String message;
  public Hex(String val) {
    message = val;
  }
  /**
   * Converts Hexadecimal into a binary
   *
   * @param hex
   * @return
   */
  public boolean[] Hex2Bin(Hex hex) {
    boolean[] x = new boolean[1];
    return x;
  }
}
