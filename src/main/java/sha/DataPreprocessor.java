package sha;

public class DataPreprocessor {

  public DataPreprocessor() {}
  /**
   * According to the standard, the message has to be padded before.
   * @param message
   * @return boolean array
   */
  public boolean[] padding(String message) {
    boolean[] x = new boolean[0];
    return x;
  }
}
